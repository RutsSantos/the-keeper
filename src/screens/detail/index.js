import React from "react";
import { View, Text } from "react-native";

export default function Detail({ navigation }) {
    return (
        <View>
            <Text>Detail</Text>
        </View>
    )
}